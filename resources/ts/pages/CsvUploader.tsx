import React, { ReactElement } from 'react';

const CsvUploader = (): ReactElement => {
    return (
        <form>
            <input type="file" />
        </form>
    );
};

export default CsvUploader;
